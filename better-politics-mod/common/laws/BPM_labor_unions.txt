# group = this is the law_group a law belongs to
# icon = graphical icon shown in-game
# modifier = {} modifier on country for having adopted this law

REPLACE:law_right_to_associate = {
	group = lawgroup_labour_associations
	
	icon = "gfx/interface/icons/law_icons/right_to_associate.dds"

	can_enact = {
		trigger_if = { 
			limit = { NOT = { has_variable = union_law_introduced_var } }
			hidden_trigger = { always = no } # Resolved by event
			ig:ig_trade_unions = {
				OR = {
					bpm_ig_is_marginal = no
					custom_description = {
						text = bpm_tt_trade_unions_population_percentage
						interest_group_population_percentage >= 0.05
					}
				}
			}
		}
	}

	progressiveness = 50

	unlocking_technologies = {
		law_enforcement
	}

	modifier = {
		interest_group_ig_trade_unions_pol_str_mult = 0.25
		country_populist_movement_support_add = 0.05
		state_pop_support_movement_labor_mult = 0.1
	}
	
	on_activate = {
		if = {
			limit = { game_date > 1836.1.2 }
			set_variable = union_law_introduced_var
		}
		ig:ig_trade_unions ?= {
			bpm_remove_all_dop_ideologies = yes
			add_ideology = ideology_dop_unionist
			bpm_reorder_ideologies = yes
		}
		if = {
			limit = { has_variable = bpm_state_union_activated }
			trigger_event = { id = bpm_union_leadership.2 }
		}
	}

	
	ai_enact_weight_modifier = {
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

law_no_labor_unions = { 
	# Placeholder law for without unions
	# does not disable exploitative pms
	group = lawgroup_labour_associations


	icon = "gfx/interface/icons/law_icons/combination_acts.dds"

	is_visible = {
		OR = {
			has_law = law_type:law_no_labor_unions
			has_game_started = no
		}
	}
	
	can_enact = {
		always = no
	}

	progressiveness = -50

	modifier = {
	}
	
	on_activate = {
	}

	
	ai_enact_weight_modifier = {
		value = 0
	}
}

REPLACE:law_guild_system = {
	group = lawgroup_labour_associations

	icon = "gfx/interface/icons/law_icons/guild_system.dds"

	progressiveness = -50

	is_visible = {
		always = no
	}

	can_enact = {
		always = no
	}

	on_activate = {
		deactivate_law = law_type:law_guild_system
		if = {
			limit = { has_variable = union_law_introduced_var }
			activate_law = law_type:law_right_to_associate
		}
		else = {
			activate_law = law_type:law_no_labor_unions
		}
	}
	on_deactivate = {
	}

	on_enact = {
	}

	modifier = {}
	ai_will_do = {
		always = no
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0
	}
}


REPLACE:law_combination_acts = {
	group = lawgroup_labour_associations
	
	icon = "gfx/interface/icons/law_icons/violent_suppresion.dds"
	
	progressiveness = -50

	can_enact = {
		trigger_if = { 
			limit = { NOT = { has_variable = union_law_introduced_var } }
			hidden_trigger = { always = no } # Resolved by event
			ig:ig_trade_unions = { 
				OR = {
					bpm_ig_is_marginal = no
					custom_description = {
						text = bpm_tt_trade_unions_population_percentage
						interest_group_population_percentage >= 0.05
					}
				}
			}
		}
	}

	unlocking_technologies = {
		law_enforcement
	}
	
	disallowing_laws = {
		law_council_republic
		law_no_police
	}

	modifier = {
		state_political_strength_from_wealth_mult = 0.1
		interest_group_ig_trade_unions_pol_str_mult = -0.5
		interest_group_ig_trade_unions_pop_attraction_mult = -0.5
		country_bpm_ig_trade_unions_non_institutional_bool = yes
		state_pop_support_movement_labor_mult = -0.10
		state_pop_support_movement_communist_mult = 0.10
		state_pop_support_movement_anarchist_mult = 0.10
		state_pop_support_movement_socialist_mult = 0.10
	}

	on_activate = {
		if = {
			limit = { game_date > 1836.1.2 }
			set_variable = union_law_introduced_var
		}
		ig:ig_trade_unions ?= {
			bpm_remove_all_dop_ideologies = yes
			add_ideology = ideology_dop_unionist
			bpm_reorder_ideologies = yes
		}
		if = {
			limit = { has_variable = bpm_state_union_activated }
			trigger_event = { id = bpm_union_leadership.2 }
		}
	}
	
	ai_enact_weight_modifier = {
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

}

REPLACE:law_corporatized_unions = {
	group = lawgroup_labour_associations
	
	icon = "gfx/interface/icons/law_icons/state_controlled_union.dds"
	
	progressiveness = -25

	can_enact = {
		trigger_if = { 
			limit = { NOT = { has_variable = union_law_introduced_var } }
			hidden_trigger = { always = no } # Resolved by event
			ig:ig_trade_unions = { 
				OR = {
					bpm_ig_is_marginal = no
					custom_description = {
						text = bpm_tt_trade_unions_population_percentage
						interest_group_population_percentage >= 0.05
					}
				}
			}
		}
	}

	unlocking_technologies = {
		corporatism
	}
	
	modifier = {
		state_political_strength_from_wealth_mult = 0.05
		interest_group_ig_trade_unions_pop_attraction_mult = -0.1
		interest_group_ig_petty_bourgeoisie_pol_str_mult = 0.25
		bpm_decorative_trade_unions_force_leader_moderate = yes
		state_pop_support_movement_communist_mult = -0.05
		state_pop_support_movement_anarchist_mult = -0.05
		state_pop_support_movement_socialist_mult = -0.05
	}

	on_activate = {
		if = {
			limit = { game_date > 1836.1.2 }
			set_variable = union_law_introduced_var
		}
		ig:ig_trade_unions ?= {
			bpm_remove_all_dop_ideologies = yes
			add_ideology = ideology_dop_state_unionist
			bpm_reorder_ideologies = yes
		}
		set_variable = bpm_state_union_activated
		trigger_event = { id = bpm_union_leadership.1 }
	}
	
	ai_enact_weight_modifier = {
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}

REPLACE:law_anti_strike_laws = {
	group = lawgroup_labour_associations
	
	icon = "gfx/interface/icons/law_icons/limited_unions.dds"

	can_enact = {
		trigger_if = { 
			limit = { NOT = { has_variable = union_law_introduced_var } }
			hidden_trigger = { always = no } # Resolved by event
			ig:ig_trade_unions = { 
				OR = {
					bpm_ig_is_marginal = no
					custom_description = {
						text = bpm_tt_trade_unions_population_percentage
						interest_group_population_percentage >= 0.05
					}
				}
			}
		}
	}

	progressiveness = 0

	unlocking_technologies = {
		law_enforcement
	}

	modifier = {
		interest_group_ig_trade_unions_pol_str_mult = -0.25
		interest_group_ig_trade_unions_pop_attraction_mult = -0.25
		bpm_decorative_trade_unions_force_leader_non_socialist = yes
		state_pop_support_movement_labor_mult = -0.05
		state_pop_support_movement_communist_mult = 0.05
		state_pop_support_movement_anarchist_mult = 0.05
		state_pop_support_movement_socialist_mult = 0.05
	}
	
	on_activate = {
		if = {
			limit = { game_date > 1836.1.2 }
			set_variable = union_law_introduced_var
		}
		ig:ig_trade_unions ?= {
			bpm_remove_all_dop_ideologies = yes
			add_ideology = ideology_dop_unionist
			bpm_reorder_ideologies = yes
		}
		if = {
			limit = { has_variable = bpm_state_union_activated }
			trigger_event = { id = bpm_union_leadership.2 }
		}
	}



	ai_enact_weight_modifier = {
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
	
}

REPLACE:law_factory_councils = {
	group = lawgroup_labour_associations

	icon = "gfx/interface/icons/law_icons/factory_councils.dds"

	progressiveness = 100

	unlocking_technologies = {
		socialism
	}

	# Intended that this does not require a Council Republic, this is putting the cart before the horse

	can_enact = {
		is_revolutionary = yes

		trigger_if = { 
			limit = { NOT = { has_variable = union_law_introduced_var } }
			hidden_trigger = { always = no } # Resolved by event
			ig:ig_trade_unions = { 
				OR = {
					bpm_ig_is_marginal = no
					custom_description = {
						text = bpm_tt_trade_unions_population_percentage
						interest_group_population_percentage >= 0.05
					}
				}
			}
		}
	}

	on_enact = {
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_right_to_associate
				}
			}
			custom_tooltip = trade_unions_much_more_radical_effect_tt
		}
		if = {
			limit = {
				OR = {
					has_law_or_variant = law_type:law_guild_system
					has_law_or_variant = law_type:law_anti_strike_laws
					has_law_or_variant = law_type:law_corporatized_unions
				}
			}
			custom_tooltip = trade_unions_more_radical_effect_tt
		}
		if = {
			limit = {
					has_law_or_variant = law_type:law_corporatized_unions
			}
			custom_tooltip = trade_unions_less_corporatist_effect_tt
		}
	}

	on_deactivate = {
		if = {
			limit = { 
				has_law_or_variant = law_type:law_elected_police 
				NOR = {
					has_law_or_variant = law_type:law_anarchy
					has_law_or_variant = law_type:law_elected_bureaucrats
				}
			}
			activate_law = law_type:law_dedicated_police
		}
		if = {
			limit = { 
				has_law = law_type:law_elected_officers 
				NOR = {
					has_law_or_variant = law_type:law_anarchy
					has_law_or_variant = law_type:law_elected_bureaucrats
				}
			}
			activate_law = law_type:law_political_officers
		}
	}

	modifier = {
		interest_group_ig_trade_unions_pol_str_mult = -0.75
		interest_group_ig_trade_unions_pop_attraction_mult = -0.75
		state_pop_support_movement_labor_mult = -0.10
		state_pop_support_movement_communist_mult = 0.20
		state_pop_support_movement_anarchist_mult = 0.20
		building_group_bg_manufacturing_allowed_collectivization_add = 0.5
		building_group_bg_extraction_allowed_collectivization_add = 0.5
		building_group_bg_power_allowed_collectivization_add = 0.5
		building_group_bg_private_infrastructure_allowed_collectivization_add = 0.5
		building_group_bg_arts_allowed_collectivization_add = 0.5
	}

	ai_will_do = {
		always = no
	}

	ai_enact_weight_modifier = { #Petitions
		value = 0

		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}


law_syndicalism = {
	group = lawgroup_labour_associations
	
	icon = "gfx/interface/icons/law_icons/syndicalism.dds"

	progressiveness = 50

	can_enact = {
		trigger_if = { 
			limit = { NOT = { has_variable = union_law_introduced_var } }
			hidden_trigger = { always = no } # Resolved by event
			ig:ig_trade_unions = { 
				OR = {
					bpm_ig_is_marginal = no
					custom_description = {
						text = bpm_tt_trade_unions_population_percentage
						interest_group_population_percentage >= 0.05
					}
				}
			}
		}
	}

	unlocking_technologies = {
		anarchism
	}

	unlocking_laws = {
		law_council_republic
	}
	
	modifier = {
		interest_group_ig_trade_unions_pol_str_mult = 0.5
		interest_group_ig_petty_bourgeoisie_pol_str_mult = 0.5
		country_machinists_pol_str_mult = 0.5
		country_laborers_pol_str_mult = 0.5
		country_engineers_pol_str_mult = 0.5
		country_bureaucrats_pol_str_mult = 0.25
		country_peasants_pol_str_mult = -0.5
		country_farmers_pol_str_mult = -0.5
	}
	
	on_activate = {
		if = {
			limit = { game_date > 1836.1.2 }
			set_variable = union_law_introduced_var
		}
		ig:ig_trade_unions ?= {
			bpm_remove_all_dop_ideologies = yes
			add_ideology = ideology_dop_syndical_unionist
			bpm_reorder_ideologies = yes
		}
		if = {
			limit = { exists = ig:ig_anarchists }
			ig:ig_anarchists ?= {
				bpm_remove_all_dop_ideologies = yes
				add_ideology = ideology_dop_anarcho_syndicalist
				bpm_reorder_ideologies = yes
			}
		}
		if = {
			limit = { has_variable = bpm_state_union_activated }
			trigger_event = { id = bpm_union_leadership.2 }
		}
	}
	
	ai_enact_weight_modifier = {
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}

}

law_socialist_labor_unions = {
	group = lawgroup_labour_associations
	
	icon = "gfx/interface/icons/law_icons/socialist_unions_only.dds"

	progressiveness = 50

	can_enact = {
		trigger_if = { 
			limit = { NOT = { has_variable = union_law_introduced_var } }
			hidden_trigger = { always = no } # Resolved by event
			ig:ig_trade_unions = { 
				OR = {
					bpm_ig_is_marginal = no
					custom_description = {
						text = bpm_tt_trade_unions_population_percentage
						interest_group_population_percentage >= 0.05
					}
				}
			}
		}
	}

	unlocking_technologies = {
		corporatism
	}

	unlocking_laws = {
		law_council_republic
	}
	
	modifier = {
		interest_group_ig_trade_unions_pol_str_mult = 0.25
		interest_group_ig_petty_bourgeoisie_pol_str_mult = 0.25
		bpm_decorative_trade_unions_force_leader_vanguardist = yes
	}
	
	on_activate = {
		if = {
			limit = { game_date > 1836.1.2 }
			set_variable = union_law_introduced_var
		}
		ig:ig_trade_unions ?= {
			bpm_remove_all_dop_ideologies = yes
			add_ideology = ideology_dop_unionist
			bpm_reorder_ideologies = yes
		}
		if = {
			limit = { has_variable = bpm_state_union_activated }
			trigger_event = { id = bpm_union_leadership.2 }
		}
	}

	ai_enact_weight_modifier = {
		value = 0
		
		if = {
			limit = { ai_has_enact_weight_modifier_journal_entries = yes }
			add = 750
		}
	}
}